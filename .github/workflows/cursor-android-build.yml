name: Cursor Android prototype

on:
  pull_request:
    branches: [main]
    paths:
      - 'cursor-android/**'
      - '.github/workflows/cursor-android-build.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke
        run: echo "Cursor Android workflow is active"
