"use client";

// Moved to @cesium/client/react (packages/client/src/react/UserPreferencesProvider.tsx). Re-export shim keeps existing imports stable.
export {
  UserPreferencesProvider,
  useUserPreferences,
} from "@cesium/client/react";
