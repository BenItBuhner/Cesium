"use client";

// Moved to @cesium/client/react (packages/client/src/react/GlobalSettingsProvider.tsx). Re-export shim keeps existing imports stable.
export {
  GlobalSettingsProvider,
  useGlobalSettings,
} from "@cesium/client/react";
