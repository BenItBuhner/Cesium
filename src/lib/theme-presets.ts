// Moved to @cesium/client (packages/client/src/theme-presets.ts). Re-export shim keeps existing imports stable.
export {
  BUILTIN_THEME_CATALOG,
  DEFAULT_BUILTIN_THEME_ID,
  isBuiltinThemeId,
  listBuiltinThemeIds,
} from "@cesium/client";
export type {
  BuiltinThemePreset,
} from "@cesium/client";
