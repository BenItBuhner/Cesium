// Moved to @cesium/client (packages/client/src/rail-fetch.ts). Re-export shim keeps existing imports stable.
export {
  RAIL_FETCH_TIMEOUT_MS,
  RAIL_INITIAL_LOAD_FAILSAFE_MS,
  resolveRailFetchServers,
  withRailFetchTimeout,
} from "@cesium/client";
