// Moved to @cesium/client (packages/client/src/desktop-environment.ts). Re-export shim keeps existing imports stable.
export {
  isCesiumDesktopApp,
  reloadAppWindow,
  useCesiumRendererFeatureFlags,
  useIsCesiumDesktopApp,
} from "@cesium/client";
