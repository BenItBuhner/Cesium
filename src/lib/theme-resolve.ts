// Moved to @cesium/client (packages/client/src/theme-resolve.ts). Re-export shim keeps existing imports stable.
export {
  normalizeThemeIdForConfig,
  resolveActiveThemeId,
  resolveColorSchemeDark,
  resolveMergedTokens,
} from "@cesium/client";
