// Moved to @cesium/core (packages/core/src/ask-question-utils.ts). Re-export shim keeps @/lib/ask-question-utils imports stable.
export {
  askStepsFromMessage,
  normalizeQuestionOptions,
} from "@cesium/core";
