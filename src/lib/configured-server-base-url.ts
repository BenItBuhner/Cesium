// Moved to @cesium/client (packages/client/src/configured-server-base-url.ts). Re-export shim keeps existing imports stable.
export {
  getConfiguredServerBaseUrl,
  getConfiguredServerPort,
  isLoopbackServerBaseUrl,
} from "@cesium/client";
