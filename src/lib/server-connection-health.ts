// Moved to @cesium/client (packages/client/src/server-connection-health.ts). Re-export shim keeps existing imports stable.
export {
  probeServerBaseUrl,
} from "@cesium/client";
export type {
  ServerProbeResult,
} from "@cesium/client";
